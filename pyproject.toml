[project]
name = "advanced-benchmark"
version = "0.1.0"
description = "Benchmark authoring workflow for scientific AI evaluation."
requires-python = ">=3.11"
dependencies = [
  "PyYAML>=6.0",
  "pypdfium2>=4.30.0",
  "Pillow>=10.0.0",
]

[tool.uv]
package = false
